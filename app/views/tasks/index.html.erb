<h1>My Tasks</h1>
<% @tasks.each do |task| %>
  <h2><%= task.title %></h2>
  <p><%= task.details %></p>
  <p><%= task.completed %></p>
  <% if task.completed %>
    <input type="checkbox" id="/" name="/" checked>
  <% else %>
      <input type="checkbox" id="/" name="/">
  <% end %>
<% end %>
